# Terminal Command History Log

This log documents the essential command-line interface (CLI) utilities used during the initialization and configuration of the Week 3 environment.

## Verified CLI Commands Executed

* `mkdir iyf-s11-week-03-francooxo` — Created a dedicated workspace folder for isolation.
* `cd iyf-s11-week-03-francooxo` — Navigated directly into the project directory root.
* `git init` — Instantiated a blank local Git repository context.
* `echo` — Formatted structural text directly into standard file outputs.
* `git status` — Audited tracked vs untracked file allocations within the tree.
* `git checkout -b <branch>` — Provisioned an isolated tracking branch layout.* git log --oneline - Viewed a compressed history graph map.
